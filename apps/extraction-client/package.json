{
  "name": "@voxelize/extraction-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --clearScreen false",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vite": "^5.4.14",
    "vitest": "^1.6.1"
  }
}
